class Solution {
    public int countNegatives(int[][] grid) {
        int flag=0;
        for(int i=0;i<grid.length;i++)
        {
            for(int j=0;j<grid.length;j++)
            {
                if(grid[i][j]<0)
                flag++;
            }
        }
        return flag;
    }
}